<Application 
	xmlns="https://github.com/avaloniaui"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:conv="using:FlomtManager.UI.Converters"
	xmlns:local="using:FlomtManager.UI"
	xmlns:vm="using:FlomtManager.UI.ViewModels"
	x:Class="FlomtManager.UI.App"
	x:DataType="vm:ApplicationViewModel"
	x:CompileBindings="True"
	RequestedThemeVariant="Dark">
	
	<Application.DataTemplates>
		<local:ViewLocator />
	</Application.DataTemplates>

	<Application.Styles>
		<StyleInclude Source="avares://Semi.Avalonia/Themes/Index.axaml" />
		<StyleInclude Source="avares://Semi.Avalonia.DataGrid/Index.axaml" />
		<StyleInclude Source="avares://Semi.Avalonia.ColorPicker/Index.axaml" />
	</Application.Styles>

	<Application.Resources>
		<conv:AddEditConverter x:Key="AddEditConverter" />
	</Application.Resources>
	
	<TrayIcon.Icons>
		<TrayIcons>
			<TrayIcon Icon="/Assets/avalonia-logo.ico" ToolTipText="Fromt Manager">
				<TrayIcon.Menu>
					<NativeMenu>
						<NativeMenuItem Header="Show" Command="{Binding TrayIconClickedCommand}" />
						<NativeMenuItem Header="Exit" Command="{Binding ExitCommand}" />
					</NativeMenu>
				</TrayIcon.Menu>
			</TrayIcon>
		</TrayIcons>
	</TrayIcon.Icons>
</Application>